import { SelectedishPipe } from './selectedish.pipe';

describe('SelectedishPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedishPipe();
    expect(pipe).toBeTruthy();
  });
});
